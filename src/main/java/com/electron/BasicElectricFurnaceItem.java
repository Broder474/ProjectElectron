package com.electron;

import net.minecraft.block.Block;
import net.minecraft.item.BlockItem;

public class BasicElectricFurnaceItem extends BlockItem {
    public BasicElectricFurnaceItem(Block block, Settings settings) {
        super(block, settings);
    }
}
